name: cut_less
run-name: mugi
on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: http://git.lovemugi.cloud/mugi/checkout@v4
      - name: Build Rust server
        working-directory: ./docker
        run: bash pack.sh

          
          